Required credentials:
- CAL_API_KEY: Cal.com API key from Settings > Security.

Optional inputs:
- SCHEDULE_ID: ID returned from creating the schedule.
- DEFAULT_AVAILABILITY_ID: ID returned in the schedule response for the default 9-5 availability.
